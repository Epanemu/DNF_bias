# @package _global_
# scenario: constant_subclass
# n_samples: ${hydra:runtime.choice}
# dimension: ${hydra:runtime.choice}
# k: 3
# rho: null

# hydra:
#   sweeper:
#     params:
#       dimension: 1, 2, 3, 4, 5, 6, 7, 8
#       n_samples: 10, 50, 100, 500, 1000, 5000, 10000, 50000
# run this as: python experiment.py +method=<method> +experiment=synthetic -m



# @package _global_
scenario: constant_subclass
n_samples: ${hydra:runtime.choice}
dimension: ${hydra:runtime.choice}
k: 4
rho: null

hydra:
  sweeper:
    params:
      dimension: 4
      n_samples: 50
# run this as: python experiment.py +method=brcg +experiment=synthetic -m
