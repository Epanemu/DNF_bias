# @package _global_
scenario: ${hydra:runtime.choice}
n_samples: ${hydra:runtime.choice}

hydra:
  sweeper:
    params:
      scenario: stress, insurance_uninsured, insurance_private, home_problems, socializing
      n_samples: 1000, 5000, 10000, 50000 # there is ~27k rows, so the 50k will take all - should be independent of the seed then.

# run this as: python experiment.py +method=<method> +experiment=MEPS -m
